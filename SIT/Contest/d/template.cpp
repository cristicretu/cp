#include <bits/stdc++.h>

int main() {
  std::ios_base::sync_with_stdio(false);
  std::cin.tie(nullptr);

  std::string s;
  int n, a, b;
  std::cin >> n >> a >> b;
  for (int i = 0; i < n; ++i) s.push_back('0');
}