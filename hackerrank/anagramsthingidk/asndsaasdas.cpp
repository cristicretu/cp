#include <bits/stdc++.h>

int main() {
  std::ios_base::sync_with_stdio(false);

  int q;
  std::cin >> q;
  while (q--) {
    std::string s;
    std::cin >> s;
    // brute force boi
    for (i) }
  return 0;
}
